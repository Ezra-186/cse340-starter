<section class="account-update">
    <div class="container">
        <h1 class="page-title">
            <%= title %>
        </h1>

        <% if (message && message.length) { %>
            <div class="notice">
                <%= message[0] %>
            </div>
            <% } %>

                <% if (errors && errors.length) { %>
                    <ul class="notice">
                        <% errors.forEach(function (error) { %>
                            <li>
                                <%= error.msg %>
                            </li>
                            <% }) %>
                    </ul>
                    <% } %>

                        <h2>Account Update</h2>

                        <form action="/account/update" method="post" class="form" id="accountUpdateForm">
                            <input type="hidden" name="account_id" value="<%= account_id %>" />

                            <label for="account_firstname">First name</label>
                            <input type="text" id="account_firstname" name="account_firstname" required
                                value="<%= typeof account_firstname != 'undefined' ? account_firstname : '' %>" />

                            <label for="account_lastname">Last name</label>
                            <input type="text" id="account_lastname" name="account_lastname" required
                                value="<%= typeof account_lastname != 'undefined' ? account_lastname : '' %>" />

                            <label for="account_email">Email</label>
                            <input type="email" id="account_email" name="account_email" required
                                value="<%= typeof account_email != 'undefined' ? account_email : '' %>" />

                            <button type="submit">Save account</button>
                        </form>

                        <h2>Change Password</h2>

                        <form action="/account/update-password" method="post" class="form" id="passwordUpdateForm">
                            <input type="hidden" name="account_id" value="<%= account_id %>" />

                            <label for="account_password">New password</label>
                            <input type="password" id="account_password" name="account_password" required
                                minlength="12" />

                            <p class="helper-text">
                                Enter a new password to replace your current one. It should follow the
                                same rules as registration.
                            </p>

                            <button type="submit">Change password</button>
                        </form>
    </div>
</section>